
/// <reference types="vite/client" />
/// <reference types="react" />
/// <reference types="react-dom" />

declare global {
  // Fix for React 19 / Vite regex type issue if present, but mainly just ensuring React is loaded
}
